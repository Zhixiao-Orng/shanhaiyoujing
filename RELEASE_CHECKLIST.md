# v0.1.0 发布检查

## 发布内容

- [x] 明确为本地源码试用版；包含安装说明、Skill 和网页指南。
- [x] 发布包排除个人任务数据库、日志、依赖目录和私人绝对路径。
- [x] 保留上游 MIT 许可证和来源说明。
- [x] 增加本地版核心测试，执行类型检查与构建。
- [x] 核查本地访问边界、Markdown、命令调用和数据库升级代码。
- [x] 用户确认新版 Windows 测试通过；未在本机独立复核报告日志。
- [ ] 上传 GitHub 后为对应提交建立 v0.1.0 标签；当前文件包不代表已发布到 GitHub。

## 安全核查结果与范围

server/app.mjs 原先信任私网网页来源，并保留云连接及 AI 路由。现仅绑定 127.0.0.1，校验回环 Host 与同源 Origin（Codex app://- 为明确例外；--dev 允许回环开发端口），关闭 /api/local/*、设备目录、工作流和开发上下文路由，忽略旧云端配置。无 Origin 的本机 CLI 请求仍可使用；不是针对恶意本机进程的沙箱。

TaskDetail 与 UserGuide 使用 react-markdown 默认 URL 处理，未启用 raw HTML 插件或 dangerouslySetInnerHTML；测试确认 HTML 不作为标签执行、javascript 链接不输出可执行地址。未声称实施全面渗透测试或依赖漏洞认证。

app.mjs 的 git execFile 和 Codex spawn 使用参数数组，未启用 shell；其扫描/调用入口已在当前发布版关闭。启动和同步脚本仍需执行本机 Node，这属于受信任安装操作。此检查不覆盖用户另行运行的上游内部工具。

数据库有建表、缺失列补齐、任务状态迁移，部分步骤有事务，并非“没有迁移”。新增测试覆盖重复打开保留数据、缺失项目字段升级；未证明任意历史数据库均可升级。升级前必须保留停止服务后的完整备份。

## 测试说明

npm test 明确运行 test/release.test.mjs，覆盖当前本地发布范围。上游开发目录另有旧测试，涉及已删除的 AI/云端/工作流功能；直接运行无范围 node --test 会执行它们，存在失败，不计为本地版全套通过。此前发布包没有带测试目录，导致 npm test 显示零用例；本包已修正。

触控板拖动、缩放和完整 Windows Skill 加载仍需人工验证；不把 API 测试说成 UI 端到端测试。
